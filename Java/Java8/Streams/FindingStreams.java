package Java.Java8.Streams;

/**
 * Demonstrates whether some elements in a set of data match a given property
 * 
 * methods: allMatch(), anyMatch(), noneMatch(), findFirst(), findAny()
 */
public class FindingStreams {
    
}
